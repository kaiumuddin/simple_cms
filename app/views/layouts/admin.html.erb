<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple CMS : <%= @page_title || "Admin Area"%></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>
<body class="flex items-center justify-center min-h-screen bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 text-white">

    <% if !flash[:notice].blank? %>
        <div class="mb-4 p-4 bg-green-500 text-green-100 rounded shadow-lg">
            <%= flash[:notice] %>
        </div>
    <% end %>

    <!-- before yield -->

    <div class="bg-white text-gray-800 rounded shadow-lg p-8 w-full max-w-2xl">
        <%= yield %>
    </div>

    <!-- after yield -->

</body>
</html>
